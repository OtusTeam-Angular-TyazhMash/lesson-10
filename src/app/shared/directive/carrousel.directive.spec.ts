import { CarrouselDirective } from './carrousel.directive';

describe('CarrouselDirective', () => {
  it('should create an instance', () => {
    const directive = new CarrouselDirective();
    expect(directive).toBeTruthy();
  });
});
